# `sim/`

Integral simulation time: `Tick` is a point, `TickSpan` a duration,
`TickRate` the only seconds boundary.

```{doxygenclass} moba_sim::Tick
:members:
```

```{doxygenclass} moba_sim::TickSpan
:members:
```

```{doxygenclass} moba_sim::TickRate
:members:
```

```{doxygenvariable} moba_sim::kSimulationStart
```

```{doxygenvariable} moba_sim::kDefaultTickRate
```
